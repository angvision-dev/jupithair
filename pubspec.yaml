name: jupithair

description: Jupithair flutter app

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2

  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_riverpod: ^2.6.1
  flutter_keyboard_visibility: ^6.0.0
  supabase_flutter: ^2.9.0
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

assets:
  - .env
